let arr = [2, 5, 8, 9, 4, 3]

let serch = (arr, element, start, end) => {
    mid = (start + end / 2)
    if (arr[mid]==element) {
        return mid;
    }
    if (arr[mid] == element) {
        return mid;
    }
    if (arr[mid] > element) {
        
    }
}